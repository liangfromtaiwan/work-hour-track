# Supabase → Project Settings → API (use your project's URL and anon key).
NEXT_PUBLIC_SUPABASE_URL=https://skncrqljzromxsbtntku.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Must match a row in public.reports.share_token (seed uses demo-report).
NEXT_PUBLIC_DEFAULT_SHARE_TOKEN=demo-report
